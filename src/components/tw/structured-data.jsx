/**
 * Structured data is statically embedded in index.html for instant crawler discovery.
 * This component returns null to avoid duplicate script injection.
 */
export function StructuredData() {
  return null;
}
